// lib/db.tnt
//
// Shared database module. Files in lib/ are automatically available
// in all route files as modules (e.g., `db.find_user(id)`).

fn find_user(id) {
    // Mock implementation
    if id == "1" {
        return Some(map { "id": 1, "name": "Alice" })
    }
    return None
}

fn find_all_users() {
    return [
        map { "id": 1, "name": "Alice" },
        map { "id": 2, "name": "Bob" }
    ]
}
