// routes/api/status.tnt → GET /api/status

import { json } from "std/http/server"

fn get(req) {
    return json(map {
        "status": "ok",
        "version": "0.1.7",
        "routing": "file-based"
    })
}
