import { json } from "std/http/server"

fn get(req) {
    let id = req.params["id"]
    return json(map { "id": id })
}

fn put(req) {
    let id = req.params["id"]
    return json(map { "updated": id })
}

fn delete(req) {
    let id = req.params["id"]
    return json(map { "deleted": id })
}
