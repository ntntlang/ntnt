// Intent Language - Contract Violation Examples
// ==============================================
// This file demonstrates what happens when contracts are violated.
// Each example will fail at runtime with a clear error message.

// Test 1: Precondition Violation
// ------------------------------
fn divide(a, b)
    requires b != 0
{
    return a / b;
}

print("Testing precondition violation...");
print(divide(10, 0));  // This should fail!
